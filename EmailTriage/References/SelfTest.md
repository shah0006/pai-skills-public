## Self-test

```bash
bun run ~/.claude/skills/EmailTriage/Tools/Doctor.ts
```

Verifies bun runtime, layout, frontmatter, schema validity, plist syntax, dependencies, and AppleMail availability. Exit 0 on clean pass, 1 on any FAIL.
