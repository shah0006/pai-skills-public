---
created: 2026-05-18
modified: 2026-05-18T16:15:00-04:00
document-type: project-plan
status: ready-for-cursor-handoff
area: technology
topics:
  - pai
  - email-triage
  - skill-refactor
  - gws-transport
  - v5-upgrade
  - cursor-brief

tags:
  - project/active
  - pai
  - email
  - automation
  - skill/v5
  - cursor-handoff

summary: "Cursor-consumable brief for the EmailTriage v5 upgrade. Derived from the human-facing master plan (02 - Morning Email Triage - v5 Upgrade Plan.md). Scope: all four remaining phases (Phase 1 close through Phase 4 seal) executed autonomously with minimal sign-off gates, per user directive Q-C1 (2026-05-18). SKILL.md gap fixes scoped to 3 structural items per Q-C2 (effort field, H1 rename, Workflow Routing table). Cursor pre-answers decisions where defaults are pre-approved below; appends discovered questions to the Questions Discovered log instead of blocking. User supervises asynchronously."
---

# 02.1 - Morning Email Triage - v5 Upgrade Plan (Cursor Brief)
> [!info]+ For Cursor
> This is YOUR working brief. Execute the four phases below in order. Pre-approved defaults let you proceed without asking. When you hit a genuinely new decision: log it in the "Questions Discovered" section at the bottom and keep moving with your best-judgment default (also noted in the log). The human supervisor reviews asynchronously and redirects if needed.

## Cursor Setup Block
| Item | Value |
|---|---|
| **Workspace** | `~/.claude/skills/EmailTriage/` |
| **Git remote** | `https://github.com/shah0006/pai-email-triage.git` (origin) |
| **Current branch** | `phase-1-gws-transport` (PR #2 open against `main`) |
| **Test command** | `bun test` (run from workspace root; target: 472+ pass / 0 fail) |
| **Doctor command** | `bun run ~/.claude/skills/EmailTriage/Tools/Doctor.ts` (target: all probes OK) |
| **Lint** | TypeScript: `bun check` or per-file. Markdown: vault VaultLint runs auto on save |
| **Branch convention** | `phase-N-{slug}` (e.g. `phase-2-reconciler`, `phase-3-ui-integration`, `phase-4-cleanup`) |
| **PR target** | All PRs against `main` of `shah0006/pai-email-triage` |
| **Preferences file (user-side)** | `~/.claude/PAI/USER/SKILLCUSTOMIZATIONS/EmailTriage/preferences.yaml` |
| **Skill source-of-truth** | `~/.claude/skills/EmailTriage/SKILL.md` |
| **Questions log location** | This file, "Questions Discovered" section at bottom |

## Scope (locked)
**Execute Phases 1 close-out + 2 + 3 + 4 in sequence, autonomously.** Per user directive Q-C1: "give it as many tasks to do as possible and as many phases to complete as possible. Pre-answer questions and leave questions it discovers along the way so it does not have to stop and get permission all the time."

Estimated effort: 7-10 hours total, multi-session OK. Sign-off gates are minimal (see "Genuine sign-off gates" section).

## Pre-approved defaults (Cursor proceeds with these without asking)
These are decisions PAI/user pre-locked so Cursor can execute uninterrupted. Each carries the rationale.

| ID | Default decision | Rationale | Phase |
|---|---|---|---|
| **D1** | Mail.app/Gmail coexistence → set `mail_app_gmail_coexists: true` in `~/.claude/PAI/USER/SKILLCUSTOMIZATIONS/EmailTriage/preferences.yaml` if the file doesn't have it. Don't ask user to remove Gmail from Mail.app. | Commit 6 added this escape hatch (`9fab41c`) specifically so the user wouldn't have to manually remove Gmail from Mail.app. The preference is the lower-friction path. | Phase 1 close |
| **D2** | If Doctor probe 9b (`gws auth status`) reports FAIL → STOP at Phase 1 (don't proceed to Phase 2) and log to Questions Discovered with the exact `gws gmail auth login` command for the user to run. Don't attempt the auth flow yourself (it's browser-interactive). | gws auth is OAuth-via-browser; not Cursor-executable. User must do U1. | Phase 1 close |
| **D3** | Phase 2 reconciler sub-proposal — Cursor authors the 1-page sub-proposal AND proceeds with BUILD using its best-judgment defaults documented IN the sub-proposal. Don't pause for user sign-off; flag the defaults loudly in Questions Discovered. | Per Q-C1, minimize sign-off gates. User reviews async and can redirect if a default is wrong. | Phase 2 |
| **D4** | Phase 2 reconciler — drift detection protocol: SQLite is rendered truth; on drift detected (Gmail label state ≠ SQLite stage), the remote (Gmail label) wins for "current state" and SQLite is updated to match. Banner in next morning's triage note surfaces the drift event for the user to acknowledge. | Default per AppleMail v5.1 cohort pattern (remote = authority for transport-owned state). | Phase 2 |
| **D5** | Phase 2 reconciler — conflict resolution: if SQLite and remote disagree on `read` status, set to `unread`. If they disagree on `stage`, defer to remote. Log every conflict to a new table `reconciliation_log` (add to schema). | Errs toward user-noticeable when uncertain. Conservative. | Phase 2 |
| **D6** | Phase 3 UI — "Open in source client" routing: iCloud → `message:` URL into Mail.app; Gmail → Gmail web URL `https://mail.google.com/mail/u/0/#inbox/{messageId}`. | Specified in the master plan §"Phase 3 — pending (preview only)"; no ambiguity. | Phase 3 |
| **D7** | Phase 3 UI — degraded-mode rendering when a remote transport is unreachable: show the row from SQLite with a warning badge "⚠️ remote unreachable — last sync {timestamp}". Don't hide the email. | SQLite-as-rendered-truth principle (Q3=B). | Phase 3 |
| **D8** | Phase 4 SKILL.md cleanup → fix ONLY the 3 structural items per Q-C2 Option B: (1) add `effort: medium` frontmatter, (2) rename `# SKILL` heading to `# EmailTriage`, (3) add `## Workflow Routing` mapping table. DO NOT add Voice Notification section, Execution Log section, or slim down extra sections (Edge Cases / Self-test / Tests / Key source files / Dependencies / Related Skills). | User explicitly scoped Q-C2 to Option B. | Phase 4 |
| **D9** | Workflows/Transport.md content — explain the per-account routing (Gmail → gws, iCloud → apple-mail.sh), the `Transport` interface, atomic moveToStage on Gmail, the single-writer discipline, why iCloud stays on Mail.app, and the reconciler's role. Target audience: future contributor onboarding from scratch. ≈200-300 lines. | Master plan §"Phase 4 acceptance probes" specifies: "New contributor can onboard from Workflows alone". | Phase 4 |
| **D10** | Commit cadence: one logical change per commit. Phase boundary = PR. Test must pass on every commit. Conventional-commit format: `Phase N commit M: <verb> <object>`. | Matches existing pattern in `phase-1-gws-transport` (commits 1-6). | All phases |
| **D11** | After each phase commit batch lands: tag with `phase-N-complete`, then open PR against main. Don't auto-merge — leave PR open with status comment summarizing what shipped + test pass count. User merges after async review. | Matches PR #1 / PR #2 flow. | All phases |
| **D12** | Test fixtures for gws + AppleScript — Cursor extends existing fakes if they exist; if not, models them after `GwsGmailTransport`'s contract test (per master plan R4 mitigation). | Master plan §R4 specifies "Transport interface contract test that both implementations must pass". | All phases |
| **D13** | PR merges between phases: Cursor does NOT wait for user merge before starting the next phase. Cursor branches phase N+1 off the phase N branch tip (not main). When phase N's PR eventually merges to main, Cursor rebases the open phase N+1 branch onto main and force-pushes (`git push --force-with-lease`). Keeps the chain moving without idle time. | Q-C1 directive: work autonomously, supervisor reviews async. | All phases |
| **D14** | Test failure during a commit: Cursor does NOT abandon the work. Cursor (a) reverts the breaking commit locally with `git reset --soft HEAD^`, (b) investigates root cause, (c) fixes in-place, (d) re-commits with the same message + `(retry N)` suffix. If 3 retries fail on the same commit, log to Questions Discovered as BLOCKED and skip to the next independent task in the phase. | Don't lose progress on transient/test failures. 3-strike rule prevents infinite retry. | All phases |
| **D15** | PR description template: `## Summary` (1-2 sentences) + `## Tests` (count pre/post) + `## Doctor` (probe results) + `## Acceptance` (checklist from this brief, copy-pasted) + `## Questions for review` (link to Questions Discovered section). Cursor authors this when opening every PR. | Gives user a one-screen review surface; reduces back-and-forth. | All phases |
| **D16** | Unfamiliar pattern in existing code: Cursor studies the surrounding 2-3 files for context, then proceeds with the locally-consistent style. If the pattern conflicts with documented conventions in `~/.claude/skills/EmailTriage/References/` or `~/.claude/PAI/DOCUMENTATION/`, the documented convention wins — Cursor refactors the locally-inconsistent code in a separate commit + flags in Questions Discovered. | Pre-empts "is this style intentional?" stops. | All phases |
| **D17** | If a referenced file/path doesn't exist (e.g., a workflow expects `Tools/Foo.ts` but it's missing), Cursor creates a minimal stub that satisfies the type contract + flags in Questions Discovered as "stub created — please verify intended scope." Don't stall waiting for clarification. | Forward motion default. | All phases |

## DO NOT TOUCH (constraints + invariants)
| # | Constraint | Why |
|---|---|---|
| 1 | `~/.claude/skills/AppleMail/Tools/apple-mail.sh` — byte-identical pre/post. | AppleMail is its own skill; out of scope. Acceptance probe will verify. |
| 2 | SQLite schema — no changes EXCEPT adding the new `reconciliation_log` table for Phase 2 (D5). | Master plan §"Out of scope" forbids schema redesign. New tables for net-new features are OK; modifying existing tables is not. |
| 3 | 8-tier classification funnel logic. | Out of scope. |
| 4 | AI classifier code. | Out of scope. |
| 5 | Scheduled-sends flow (launchd + JSON packages). | Out of scope. |
| 6 | One-click unsubscribe flow. | Out of scope. |
| 7 | iMessage alerter. | Out of scope (Phase 2 may USE it for auth-failure alerts; don't modify the alerter itself). |
| 8 | Rules engine (DB-backed). | Out of scope. |
| 9 | iCloud handling — stays on `apple-mail.sh` indefinitely. | Out of scope. |
| 10 | Obsidian plugins — don't install or modify. | Out of scope. |
| 11 | `~/.claude/skills/GoogleWorkspaceCLI/` — read-only consumer; don't modify gws internals. | Sister skill; coordinate via its public interface only. |

## Phase 0 (Cursor's first task): Anticipate everything
> [!important]+ Front-load questions so phases run uninterrupted
> Per user directive 2026-05-18 ("anticipate the questions it will have at the beginning so it can complete each phase uninterrupted... work in long stretches"): BEFORE touching any code, spend 10-15 minutes reading the entire workspace + this brief + the master plan + the Phase 0/1 Design + the Gmail Transport Proposal. Enumerate EVERY question you can think of for ALL FOUR PHASES. Populate them in the "Cursor's Anticipated Questions" section below, organized per-phase, with your best-judgment default for each.
>
> Then commit a single PR-style update to this brief (`Phase 0 commit 0: anticipated questions enumerated`). User reviews async, overwrites any default that's wrong, and you read the updated defaults on next session. THIS IS THE ONE PRE-PHASE STOP. After this, you run continuously through all four phases using your defaults + the user's overwrites, only stopping for genuine blockers per the "Genuine sign-off gates" section.

### Anticipate-phase deliverable
- [x] Read: this brief end-to-end ✅ (2026-05-18)
- [x] Read: master plan `[[02 - Morning Email Triage - v5 Upgrade Plan]]` ✅ (project copy + key sections)
- [ ] Read: `[[EmailTriage Phase 0 Pre-Decisions]]` + `[[EmailTriage Phase 1 Design]]` + `[[EmailTriage Gmail Transport Proposal]]` (in `30 - Areas/Computer/Artificial Intelligence/Agents/PAI/Coordination/`) — **Obsidian links not resolved in Cursor workspace; inferring from master plan + code. User may override defaults after reading those docs.**
- [x] Read: the EmailTriage codebase — `Tools/Transport.ts`, `Tools/GenerateTriage.ts` (partial), `Tools/Doctor.ts`, `References/schema.sql`, `SKILL.md`, `web/app/api/*` (triage + email), `tests/transport-*.ts` ✅
- [x] Read: sister skill surfaces — `Doctor.ts` probes + `Transport.ts` gws/apple-mail wiring ✅
- [x] Populate "Cursor's Anticipated Questions" section below with per-phase questions + best-judgment defaults ✅
- [x] Commit: `Phase 0 commit 0: anticipated questions enumerated` (mirrored copy under `References/` in `pai-email-triage` repo + push)
- [x] Push to current branch, comment on PR #2 with "Anticipated questions posted — proceeding with defaults unless overridden in next 24h"

After commit 0 lands, proceed to Phase 1 close-out WITHOUT waiting. User async-overwrites defaults you got wrong.

## Cursor's Anticipated Questions
> Cursor populates this in the Phase 0 anticipate step. Format per entry:
> ```
> ### AQ-N (Phase X) — {short title}
> - **Question:** {what's ambiguous in the spec or codebase}
> - **My default:** {what I'll do unless overridden}
> - **Confidence:** HIGH / MEDIUM / LOW (LOW = please review carefully)
> - **Impact if wrong:** {1 sentence — reversible? blast radius?}
> - **User override:** `{ }`
> ```
>
> Group by phase. Update timestamps when added.
>
> **Anticipate pass completed:** 2026-05-18 (Cursor). Coordination wikis in Obsidian not loaded in-repo; defaults lean on master plan `02 - … v5 Upgrade Plan`, this brief, and code inspection.

### Phase 1 close-out (AQ-1.N)

### AQ-1.1 (Phase 1) — Doctor probe 10 when Mail.app is unreachable
- **Question:** If `osascript` Mail probe throws, Doctor records **WARN** (not FAIL). Does Phase 1 still count as “all probes OK” for step 4?
- **My default:** Treat **FAIL-only** as hard stop per D2/D2-adjacent wording; **WARN** on Mail.app account check is acceptable to proceed (document in PR #2 comment). If the user wants strict green-only, they override here.
- **Confidence:** MEDIUM
- **Impact if wrong:** User merges thinking Mail state was verified when it was not; reversible (re-run Doctor with Mail open).
- **User override:** `{ }`

### AQ-1.2 (Phase 1) — Committing `preferences.yaml` to git
- **Question:** D1 edits a file under `SKILLCUSTOMIZATIONS` (typically not in repo). Phase 1 task 6 says commit preference change — should secrets/paths ever enter git?
- **My default:** **Never** `git add` `preferences.yaml`. Apply D1 on disk only; if there is no repo-tracked change, **skip** Phase 1 commit 7 (no empty commit). Note in PR #2 comment that D1 was applied locally.
- **Confidence:** HIGH
- **Impact if wrong:** Accidental PII in remote; high blast radius.
- **User override:** `{ }`

### AQ-1.3 (Phase 1) — Gmail smoke test in CI / headless agents
- **Question:** `GenerateTriage.ts --account g` needs live gws + Gmail. Cursor sandboxes may lack auth.
- **My default:** On this machine: run smoke when Doctor 9b is OK; in CI: rely on existing test suite only, skip live smoke, log “smoke requires principal host” in Questions Discovered.
- **Confidence:** HIGH
- **Impact if wrong:** False confidence from skipped smoke; mitigated by tests + Doctor.
- **User override:** `{ }`

### AQ-1.4 (Phase 1) — Doctor 9b command wording vs implementation
- **Question:** Brief text says “`gws auth status`”; `Doctor.ts` uses `gws gmail users getProfile`. Which is canonical for DQ logs?
- **My default:** Match **Doctor implementation** (`getProfile`); tell user to run **`gws gmail auth login`** on failure (per D2).
- **Confidence:** HIGH
- **Impact if wrong:** Confusing instructions only.
- **User override:** `{ }`

### Phase 2 reconciler (AQ-2.N)

### AQ-2.1 (Phase 2) — What is “SQLite stage” after `staged_emails` drop?
- **Question:** `Db.ts` migration dropped `staged_emails`. Drift detection needs a stable SQLite-side stage per message. Source = `email_actions.folder`, last triage note parse, or new read-only mirror?
- **My default:** **Primary:** derive expected stage from **latest triage note** for date D (same parser Generate uses). **Secondary:** `email_actions` / `follow_ups` for executed moves. Persist reconciler outputs only in **`reconciliation_log`** + optional banner text (no ALTER of existing tables). Document in `ReconcilerDesign.md`.
- **Confidence:** MEDIUM
- **Impact if wrong:** Reconciler compares wrong things; fixable by adjusting comparison source.
- **User override:** `{ }`

### AQ-2.2 (Phase 2) — `reconciliation_log` column shape
- **Question:** Schema not specified beyond table name.
- **My default:** `id INTEGER PK`, `created_at TEXT`, `account TEXT`, `email_id TEXT`, `conflict_type TEXT` (`stage_mismatch` | `read_mismatch` | `drift_remote_new`), `sqlite_value TEXT`, `remote_value TEXT`, `resolution TEXT`, `detail TEXT` (JSON optional). `CREATE TABLE IF NOT EXISTS` only.
- **Confidence:** MEDIUM
- **Impact if wrong:** Easy migration later (new table only).
- **User override:** `{ }`

### AQ-2.3 (Phase 2) — Pre-cron auth probe command
- **Question:** Brief says `gws gmail labels list` with 5s timeout; Doctor uses getProfile. Align?
- **My default:** Use **`gws gmail users getProfile`** with **5s** timeout for PreCron (matches auth meaning). Optionally quick `labels list` as secondary; log choice in design doc.
- **Confidence:** MEDIUM
- **Impact if wrong:** False negatives/positives on auth; tune after first run.
- **User override:** `{ }`

### AQ-2.4 (Phase 2) — “Voice + iMessage” for auth failure
- **Question:** No voice hook exists in EmailTriage today (`AlertSender` = iMessage only).
- **My default:** **Parallel:** reuse **`sendUrgentAlert`-style message** via `imessage.sh` with a synthetic one-liner urgent item **and** `curl -sk` POST to PAI **`http://localhost:31337/notify`** with `voice_enabled: true` (same pattern as PAI operational docs), guarded so tests mock both. Document env flag to disable voice in CI.
- **Confidence:** LOW
- **Impact if wrong:** Double alerts or localhost notify misses; reversible.
- **User override:** `{ }`

### AQ-2.5 (Phase 2) — Where banner text lands for “morning triage note”
- **Question:** Pre-cron runs before Generate — banner in note file prepend, frontmatter, or separate sidecar?
- **My default:** **GenerateTriage** accepts an optional **banner injection** argument / env from PreCronAuthCheck; prepend a single `> [!warning]` callout **after** YAML frontmatter of today’s note (if any) — if note doesn’t exist yet, pass banner into formatter so first write includes it.
- **Confidence:** MEDIUM
- **Impact if wrong:** Visual noise; adjust formatting once.
- **User override:** `{ }`

### AQ-2.6 (Phase 2) — iCloud drift via `apple-mail.sh`
- **Question:** Which apple-mail invocation lists mailbox placement for reconciliation?
- **My default:** Read **`apple-mail.sh --help`** and mirror existing Generate/Execute patterns; if no list-by-folder API, reconcile **only Gmail** in Phase 2 and log iCloud reconciler as “Phase 2.1 stub: inbox-only parity check” per D17 stub policy **only if** inbox-only would mislead — prefer **narrow scope** documented in design.
- **Confidence:** LOW
- **Impact if wrong:** Incomplete iCloud drift detection; Gmail path still valid per success criteria.
- **User override:** `{ }`

### AQ-2.7 (Phase 2) — D4 “SQLite rendered truth” vs “remote wins”
- **Question:** Narrative sounds contradictory.
- **My default:** **Operational truth for pipeline input = remote** after reconciler runs; **SQLite/note = UX cache** updated to match; **banner** explains user-visible correction. Wording in docs: “rendered snapshot, reconciled to remote authority.”
- **Confidence:** HIGH
- **Impact if wrong:** Doc confusion only if not explained.
- **User override:** `{ }`

### AQ-2.8 (Phase 2) — Reconciler idempotency + runtime
- **Question:** Can 5 AM job double-run or overlap Generate at 6?
- **My default:** Reconciler **read-only remote + SQLite logging** except PATCH updates nowhere (only INSERT log rows + note banner instructions). Serialize via launchd **not** overlapping: finish reconciler \< 6 AM; Generate starts after PreCron clears; use **exclusive lock file** under skill tmp if needed.
- **Confidence:** MEDIUM
- **Impact if wrong:** Rare duplicate banners; mitigation with lock file.
- **User override:** `{ }`

### Phase 3 web UI integration (AQ-3.N)

### AQ-3.1 (Phase 3) — Next.js importing `Tools/Transport.ts`
- **Question:** Skill root TS uses Bun/`spawnSync`; Next bundler may not tolerate that in client components.
- **My default:** All transport calls stay in **`app/api/*` Route Handlers** (server-only), client fetches JSON. Extract shared typed helpers into a **`web/server/transport-runner.ts`** that dynamic-imports or spawns Bun CLI if needed — **prefer subprocess** `bun run Tools/…` thin wrapper vs webpack bundling `Transport.ts`.
- **Confidence:** MEDIUM
- **Impact if wrong:** Build break; refactor boundary.
- **User override:** `{ }`

### AQ-3.2 (Phase 3) — Gmail “Open in client” URL uses API message id
- **Question:** `https://mail.google.com/mail/u/0/#inbox/{messageId}` may not resolve for Gmail REST `id` (vs thread id).
- **My default:** Implement per D6 literally first; add **fallback** query `search` Deep Link if open fails — log under DQ if user reports breakage. Optionally use **`https://mail.google.com/mail/u/0/#all/{msgId}`** as alternate.
- **Confidence:** LOW
- **Impact if wrong:** Broken deep link for some messages; manual fallback still works.
- **User override:** `{ }`

### AQ-3.3 (Phase 3) — iCloud `message:` URL target
- **Question:** Rows currently use numeric Mail ids from AppleScript list; `message:` scheme often expects **RFC5322 Message-ID** with URL encoding.
- **My default:** Try **`message://\<id\>`** only if codebase already exposes stable Message-ID header; else **`message:${numericId}`** per D6 and fix if Mail ignores — document limitation in DQ.
- **Confidence:** LOW
- **Impact if wrong:** Link opens wrong/empty; user still has web UI row context.
- **User override:** `{ }`

### AQ-3.4 (Phase 3) — “Last sync {timestamp}” provenance
- **Question:** No `last_sync` column allowed on existing tables.
- **My default:** Store ephemeral **runtime** timestamp in Next server cache / API response keyed by session **or** derive from latest triage note `generated_at` / file mtime in vault; degraded mode shows **note mtime** + account label.
- **Confidence:** MEDIUM
- **Impact if wrong:** Slightly inaccurate “sync” label; wording “last rendered” may be safer in UI copy.
- **User override:** `{ }`

### AQ-3.5 (Phase 3) — Existing bug: wrong `apple-mail.sh` path in `web/app/api/email/[id]/route.ts`
- **Question:** Route uses `~/.claude/skills/AppleMail/apple-mail.sh` but skill uses `AppleMail/Tools/apple-mail.sh`.
- **My default:** **Fix path** during Phase 3 when touching fetch layer (**do not edit** apple-mail script itself — DO NOT TOUCH #1 satisfied). Flag in DQ if behavioral change surprises anyone.
- **Confidence:** HIGH
- **Impact if wrong:** Enables iCloud reads from Review UI — positive fix.
- **User override:** `{ }`

### AQ-3.6 (Phase 3) — “Single fetch path per page” + SQLite
- **Question:** Today UI reads vault markdown via `/api/triage`; Phase 3 wants SQLite + Transport abstraction indirectly.
- **My default:** **Single aggregator API** `/api/stage-review` (name TBD) that returns merged rows per stage from SQLite-backed triage artifact + refresh hook; deprecate stray per-email shell routes **or** keep them behind the same aggregator for backward compatibility.
- **Confidence:** MEDIUM
- **Impact if wrong:** Larger refactor; still within Phase 3 scope.
- **User override:** `{ }`

### Phase 4 cleanup + docs (AQ-4.N)

### AQ-4.1 (Phase 4) — Post-phase master plan edit lives in Obsidian only
- **Question:** `00 - Morning Email Triage - Plan.md` may not be in git repo.
- **My default:** Apply **Obsidian edits on disk** at vault path user uses; EmailTriage repo gets **References/** pointer or excerpt only **if user wants** repo mirror — default **Obsidian-only** for master plan edits + DQ note.
- **Confidence:** MEDIUM
- **Impact if wrong:** Repo and vault diverge; human merges mentally.
- **User override:** `{ }`

### AQ-4.2 (Phase 4) — `apple-mail.sh` byte diff command path
- **Question:** Brief uses `git diff main -- ../AppleMail/...` from EmailTriage root — sibling path may differ in worktrees.
- **My default:** Use **absolute `$HOME/.claude/skills/AppleMail/Tools/apple-mail.sh`** vs **upstream tracked blob** comparison: `shasum` vs known good OR `git diff` from **AppleMail repo** if separate clone — log exact command used in DQ. **Do not change file.**
- **Confidence:** MEDIUM
- **Impact if wrong:** False alarm on path; rerun with correct repo layout.
- **User override:** `{ }`

### AQ-4.3 (Phase 4) — `Workflows/Transport.md` 200–300 lines vs SKILL duplication
- **Question:** Risk of drifting vs SKILL transport mentions.
- **My default:** Transport.md = **onboarding canonical**; SKILL keeps **minimal** routing table (D8); cross-link both ways in Phase 4.
- **Confidence:** HIGH
- **Impact if wrong:** Doc drift — acceptable if cross-linked.
- **User override:** `{ }`

### Cross-phase / architectural (AQ-X.N)

### AQ-X.1 (Cross-phase) — D13 (“don’t wait for merge”) vs Genuine sign-off gate #2 (“wait for merge”)
- **Question:** Contradiction in brief.
- **My default:** **D13 wins** for **branch sequencing** (stack PRs); **Gate #2** = don’t merge without human but **Cursor** still branches Phase N+1 from Phase N tip. Update gate #2 wording next edit to avoid confusion (log as DQ advisory).
- **Confidence:** HIGH
- **Impact if wrong:** Occasionally messier stacked rebase chain; `--force-with-lease` protocol per D13.
- **User override:** `{ }`

### AQ-X.2 (Cross-phase) — `--force-with-lease` vs user git safety norms
- **Question:** Force push to shared branches can be disruptive.
- **My default:** **Only** onto **phase-* feature branches** Cursor owns under D13/D11; never force-push `main`; announce in PR comment when lease-push used.
- **Confidence:** HIGH
- **Impact if wrong:** Rewrite collaborator history — mitigation: branch naming + comments only.
- **User override:** `{ }`

### AQ-X.3 (Cross-phase) — Contract test completeness for Phase 3 server adapter
- **Question:** Transport tests mock executor today; Phase 3 adds subprocess/HTTP glue.
- **My default:** Extend **existing** `tests/transport-*.ts` with adapter tests hitting **fake gws stdout** fixtures (D12); no live network in CI.
- **Confidence:** HIGH
- **Impact if wrong:** Blind spot in glue layer; add tests on failure per D14.
- **User override:** `{ }`

## Phase 1 close-out — proceed after anticipate
**Status entering:** commits 1-6 shipped on `phase-1-gws-transport`. PR #2 open. Tests 472 pass / 0 fail.

**Cursor tasks:**
1. Check current state: `git status` should be clean on `phase-1-gws-transport`. Run `bun test` — must be 472+ / 0.
2. Execute D1 (Mail.app/Gmail coexistence default):
	 - Check if `~/.claude/PAI/USER/SKILLCUSTOMIZATIONS/EmailTriage/preferences.yaml` exists.
	 - If yes, check if `mail_app_gmail_coexists:` key is present. If absent, append `mail_app_gmail_coexists: true`.
	 - If file doesn't exist, create it with `mail_app_gmail_coexists: true` (only key).
3. Run Doctor: `bun run ~/.claude/skills/EmailTriage/Tools/Doctor.ts`.
4. Branch on Doctor result:
	 - All probes OK → proceed to step 5.
	 - Probe 9b FAIL → STOP. Log to Questions Discovered: "U1 still required — user must run `gws gmail auth login` for OAuth refresh." Do NOT proceed to Phase 2.
	 - Probe 10 FAIL despite D1 → STOP. Log to Questions Discovered: "D1 default applied but probe 10 still fails — investigate why preference isn't honored. Possible bug in commit 6."
	 - Any other probe FAIL → STOP. Log to Questions Discovered with the exact probe failure.
5. Smoke test: `/generate-email-triage --account g` should produce a triage note covering Gmail without errors. (If the slash command isn't directly invokable, run the underlying `bun run Tools/GenerateTriage.ts --account g`.)
6. Commit any preference-file change with `Phase 1 commit 7: apply mail_app_gmail_coexists default per Cursor brief D1`.
7. Open or update PR #2 with a status comment summarizing: "All Doctor probes OK; smoke test passes; ready to merge."

**Phase 1 acceptance checklist:**
- [ ] `bun test` 472+/0 ✅
- [ ] `bun run Tools/Doctor.ts` all OK ✅
- [ ] `/generate-email-triage --account g` smoke test passes ✅
- [ ] PR #2 status comment posted with results ✅
- [ ] Branch pushed to origin ✅

**Phase 1 sign-off:** user merges PR #2 (async). Cursor proceeds to Phase 2 once PR #2 is merged to `main`.

## Phase 2 — Reconciler + pre-cron auth check
**Branch:** `phase-2-reconciler` off updated `main` after PR #2 merges.

**Cursor tasks:**
### 2a. Author the reconciler sub-proposal (D3)
Create `~/.claude/skills/EmailTriage/References/ReconcilerDesign.md`. Single page. Cover:
- Drift detection protocol (D4 default — document and apply).
- Conflict resolution policy (D5 default — document and apply).
- New `reconciliation_log` table schema.
- Trigger schedule (5 AM nightly cron via launchd, leveraging existing plist template pattern).
- Pre-cron auth check protocol: `gws gmail labels list` with 5s timeout BEFORE the 6 AM `/generate-email-triage`. On timeout or auth failure: skip Gmail half cleanly, write banner to morning triage note, fire voice + iMessage alert in parallel.
- Acceptance probes: (1) synthetic auth failure produces banner + alerts and clean-skip; (2) synthetic out-of-band Gmail label change is detected.

### 2b. Implement
- `Tools/PreCronAuthCheck.ts` — 5s probe + banner-write + alert-fire.
- `Tools/Reconciler.ts` — nightly drift detection comparing SQLite ↔ Gmail labels (via gws) ↔ iCloud folders (via apple-mail.sh).
- `References/schema.sql` — add `reconciliation_log` table (CREATE TABLE IF NOT EXISTS pattern; don't modify existing tables).
- `com.pai.reconciler.plist.template` — launchd template for the 5 AM job, with placeholder for vault root + EmailTriage path.
- `Tools/GenerateTriage.ts` — call PreCronAuthCheck at top; on failure, skip Gmail half cleanly.
- Test fixtures for: auth-failure scenario, out-of-band label change scenario.
- `bun test` must continue passing.

**Phase 2 acceptance checklist:**
- [ ] `bun test` passes (new tests added for both acceptance probes) ✅
- [ ] Synthetic auth failure scenario: skips Gmail cleanly, writes banner, fires alerts ✅
- [ ] Synthetic out-of-band label change: detected and surfaced in next-morning's note ✅
- [ ] `References/ReconcilerDesign.md` committed and references the implementation files ✅
- [ ] launchd template committed ✅
- [ ] PR #3 opened against main with status comment ✅

**Phase 2 sign-off:** user merges PR #3 (async). Cursor proceeds to Phase 3.

## Phase 3 — Web UI integration
**Branch:** `phase-3-ui-integration` off updated `main` after PR #3 merges.

**Cursor tasks:**
### 3a. Switch web UI to Transport abstraction over SQLite
- `web/` (Next.js on port 9988) — refactor data-fetch layer to use `transportFor(account)` indirectly via SQLite reads. UI never directly hits gws or apple-mail.sh.
- Single fetch path per page; degraded-mode tolerance via D7 default.

### 3b. "Open in source client" per email row
- Add button/icon to each row.
- Wire per D6 default:
	- iCloud rows → `message:{messageId}` URL (Mail.app handles).
	- Gmail rows → `https://mail.google.com/mail/u/0/#inbox/{messageId}`.

### 3c. Degraded-mode rendering
- If `Transport.list()` throws (remote unreachable), render from SQLite with warning badge per D7.
- Banner at top of page: "⚠️ {transport} unreachable — showing last-sync state from {timestamp}".

**Phase 3 acceptance checklist:**
- [ ] Both accounts visible in same Stage view at localhost:9988 ✅
- [ ] "Open in source client" routes correctly per account (manual smoke test) ✅
- [ ] Degraded-mode test: kill gws auth, refresh UI → degraded badge shows, page still works ✅
- [ ] `bun test` passes ✅
- [ ] PR #4 opened against main with status comment ✅

**Phase 3 sign-off:** user merges PR #4 (async). Cursor proceeds to Phase 4.

## Phase 4 — Cleanup + docs
**Branch:** `phase-4-cleanup` off updated `main` after PR #4 merges.

**Cursor tasks:**
### 4a. Verify Gmail-via-AppleScript already removed
- `grep -rE "applescript|osascript.*gmail|gmail.*applescript" ~/.claude/skills/EmailTriage/Tools/` must return 0 lines.
- Audit (2026-05-18) confirmed clean; re-verify.

### 4b. Write `Workflows/Transport.md` per D9.
### 4c. Slim SKILL.md to v5-canonical structure (per D8 — Q-C2 Option B, 3 structural fixes only)
- (1) Add `effort: medium` to frontmatter (after `context:` line).
- (2) Rename H1 from `# SKILL` to `# EmailTriage`.
- (3) Add `## Workflow Routing` section with table mapping trigger phrases to Workflow files:
	- `/generate-email-triage`, "morning triage", "start triage" → `Workflows/Generate.md`
	- `/process-email`, "execute triage", "process decisions" → `Workflows/Execute.md`
	- `/start-email-triage`, "review UI", "web ui" → `Workflows/Review.md`
	- "scheduled send", "send later" → `Workflows/ScheduledSend.md`
	- "unsubscribe", "one-click unsubscribe" → `Workflows/Unsubscribe.md`
- DO NOT add Voice Notification section, Execution Log section, or remove existing sections (Edge Cases, Self-test, Tests, Key source files, Dependencies, Related Skills).

### 4d. Verify `apple-mail.sh` byte-identical
- `git diff main -- ../AppleMail/Tools/apple-mail.sh` should be empty (we shouldn't be in that file at all).

**Phase 4 acceptance checklist:**
- [ ] Grep returns 0 lines for AppleScript-via-Gmail refs ✅
- [ ] `Workflows/Transport.md` exists and reads like onboarding doc ✅
- [ ] SKILL.md has `effort: medium`, H1 = `# EmailTriage`, `## Workflow Routing` table present ✅
- [ ] `apple-mail.sh` byte-identical pre/post ✅
- [ ] `bun test` passes ✅
- [ ] PR #5 opened against main with status comment ✅

**Phase 4 sign-off:** user merges PR #5 (async). v5 upgrade SEALED.

### Post-Phase-4 consolidation (Cursor's last task)
After PR #5 merges, Cursor adds a "Phase 17-20: v5 Upgrade" section to the master plan `00 - Morning Email Triage - Plan.md` that summarizes the v5 work (one section per phase, brief). Then this brief and the sibling `02 - Morning Email Triage - v5 Upgrade Plan.md` move to `Archive/` with a forwarding note.

## Genuine sign-off gates (Cursor MUST stop and wait)
Cursor pauses ONLY for these. Everything else uses pre-approved defaults or gets logged to Questions Discovered.

1. **U1 — `gws gmail auth login`** (Phase 1 close) — Cursor cannot execute browser OAuth. If Doctor probe 9b FAIL, log + stop.
2. **PR merge between phases** — Cursor opens the PR and waits for user merge before branching next phase. (No auto-merge.)
3. **Discovered question rated "blocking"** — if Cursor hits a decision NOT covered by pre-approved defaults AND the decision changes the architecture (locked Q1/Q2/Q3) or violates DO NOT TOUCH, stop and log. (Non-architectural questions: log + proceed with best judgment, flag in log.)

## Questions Discovered
> Cursor appends here as it works. Format per entry:
> ```
> ### DQ-N (YYYY-MM-DDTHH:MM) — {short title}
> - **Where encountered:** {file:line or phase step}
> - **The question:** {what's ambiguous}
> - **Default I applied (or "BLOCKED"):** {decision + reasoning}
> - **Why I didn't ask first:** {covered by Dn / non-architectural / low-risk}
> ```
>
> User reviews this section asynchronously. If a default is wrong, user comments inline; Cursor reads and adjusts.

### DQ-0 (2026-05-18T16:15) — Stacked PR workflow vs “wait for merge” gate
- **Where encountered:** Brief — Pre-approved defaults **D13** vs **Genuine sign-off gates §2**
- **The question:** Should Cursor stack phase branches without waiting for `main`, or pause until merge?
- **Default I applied (or "BLOCKED"):** Follow **D13** (branch Phase N+1 from Phase N tip; rebase onto `main` after merge). Treat gate §2 as “human merges, not bot” rather than blocking branch creation — align brief wording async.
- **Why I didn't ask first:** Resolved by AQ-X.1 defaults + D13 explicit table entry.

*(empty beyond DQ-0 — Cursor will populate during execution phases)*

## Reference (provenance)
- Master plan (human-facing): [[02 - Morning Email Triage - v5 Upgrade Plan]]
- Original spec: [[EmailTriage Gmail Transport Proposal]] (Q1=C/Q2=A/Q3=B locked 2026-05-04)
- Phase 0 closure: [[EmailTriage Phase 0 Pre-Decisions]]
- Phase 1 design: [[EmailTriage Phase 1 Design]] (5-commit step plan, principal-signed 2026-05-08T11:21-0400)
- Source audit: [[PAI v4 Skills Inventory]]
- Project master plan: [[00 - Morning Email Triage - Plan]] (V1+V2 build history)

## Handoff command
After Cursor reads this brief, the user kicks off with:
```bash
bun ~/.claude/PAI/TOOLS/CursorExec.ts \
  --prompt-file "/Volumes/Hyperdrive 4 Tb/Main Obsidian (Sync)/20 - Projects/Active Projects/19 - Morning Email Triage/02.1 - Morning Email Triage - v5 Upgrade Plan (Cursor Brief).md" \
  --workspace ~/.claude/skills/EmailTriage \
  --tag email-triage-v5
```

This opens a live Terminal.app window tailing the event log, pings the PAI notify endpoint, sends Telegram milestones, and saves the full stream-json transcript to `/tmp/cursor-exec-email-triage-v5.log`.
